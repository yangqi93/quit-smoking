{
  "name": "quit-smoking-miniprogram-tests",
  "version": "1.0.0",
  "description": "Unit tests for quit-smoking miniprogram",
  "scripts": {
    "test": "jest --verbose",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
